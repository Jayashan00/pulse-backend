aws_region       = "ap-south-1"        # Mumbai (closest to Sri Lanka); change as needed
instance_type    = "t3.small"
key_pair_name    = "my-ec2-keypair"    # must already exist in the region
allowed_ssh_cidr = "0.0.0.0/0"         # replace with YOUR_IP/32
